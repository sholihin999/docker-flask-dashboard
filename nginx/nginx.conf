events {}

http {
  upstream flask_app {
    server app:5000;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://flask_app;
    }

    location /api {
      proxy_pass http://flask_app/api;
    }
  }
}
